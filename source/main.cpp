#include <vtkActor.h>
#include <vtkNamedColors.h>
#include <vtkNew.h>
#include <vtkPoints.h>
#include <vtkPolyDataMapper.h>
#include <vtkProperty.h>
#include <vtkRenderWindow.h>
#include <vtkRenderWindowInteractor.h>
#include <vtkRenderer.h>
#include <vtkVertexGlyphFilter.h>

#include <sstream>
#include <iostream>

#include "Rtree.h"
using namespace std;

int main(int argc, char* argv[]) {
    /*
    const int ndimensiones = 3; // attack - defense - speed
    int M = 1; // hijos por nodo

    cout << "------- R TREE ------- " << endl;
    RTree<Pokemon, ndimensiones> test1(M);

    Record<Pokemon, ndimensiones> p1(49, 49, 45, 0.7, 4.5, 5.6);
    Record<Pokemon, ndimensiones> p2(62, 63, 60, 1, 6, 7.1);
    Record<Pokemon, ndimensiones> p3(100, 123, 80, 2, 8, 9.1);
    Record<Pokemon, ndimensiones> p4(52, 43, 65, 0.6, 3.9, 5);
    cout << "Insertando" << endl;
    test1.insert(p1);
    //test1.insert(p2);
    //test1.insert(p3);
    //test1.insert(p4);
     */
}